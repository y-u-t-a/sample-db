name: library-db Python

on:
  push:
    branches: [ main ]
    paths:
      - "library-db/mysql"
      - "!library-db/mysql/**.md"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test with pytest on container
      working-directory: library-db/mysql
      run: |
        docker-compose up --build -d db python
        docker exec -it -w /script/python python_env pytest
